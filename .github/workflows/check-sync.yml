name: check-sync
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/check-sync.py
